{
  // Wir nutzen die Basis-Konfiguration von Astro, um unnötige Strenge zu vermeiden
  "extends": "astro/tsconfigs/base", 
  "compilerOptions": {
    
    // 1. Notwendige Einstellungen für React/JSX:
    "jsx": "react-jsx",
    "jsxImportSource": "react",

    // 2. WICHTIG: Erlaubt das Ignorieren von Fehlern in Bibliotheken
    // (Löst Probleme mit event-source-polyfill, uuid, etc.)
    "skipLibCheck": true,        
    
    // 3. WICHTIG: Schaltet die strengsten Prüfungen ab (Fix für 'never' Typ)
    // Erlaubt das Nutzen von 'page: any' in den Astro-Props ohne Meckern.
    "strict": false,
    "noImplicitAny": false, 

    // 4. Stellt sicher, dass das Kompilieren gestoppt wird (Astro macht den Build)
    "noEmit": true,
    
    "allowJs": true
  }
}